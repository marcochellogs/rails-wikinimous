<div class="d-flex justify-content-center">
<div class="d-flex">
  <h1 class="mr-3"> <%= @article.title %> </h1>
  <%= link_to "   Delete", article_path(@article), method: :delete, data: { confirm: "Are you 100 per cent sure dude? This is serious stuff"} %>
  <p>- or -</p>
  <%= link_to "Edit", edit_article_path(@article)%>
</div> <br>
</div>
<div class="d-flex justify-content-center">
<p><%= @article.content %></p>
</div>

<div class="d-flex justify-content-center">
<%= link_to "Back", articles_path%>
</div>
